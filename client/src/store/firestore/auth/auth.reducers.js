let initialState = {
  user: '',
  userExists: true,
  userLoading: true,
}

const authDataList = (state = { ...initialState }, action) => {
  switch (action.type) {
    default:
      return state;
  }
}

export default authDataList;